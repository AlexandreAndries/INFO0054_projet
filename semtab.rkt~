#lang racket


(define (semtab formule_propositionnel)
  (cond (null? formule_propositionnel) (display "Pas de formule propositionnel")
        (> formule 1) (display "good job")
  
  )
)

(semtab '())